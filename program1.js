/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    const s = [];
    const brackets = {
        ')' : '(',
        '}' : '{',
        ']' : '['
    };

    for(let i of s){
        if(i in )
    }
};

module.exports = { isValid };


