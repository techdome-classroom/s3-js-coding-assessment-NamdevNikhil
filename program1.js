/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    const s = [];
    const brackets = {
        ')' : '(',
        '}' : '{',
        ']' : '['
    }
};

module.exports = { isValid };


